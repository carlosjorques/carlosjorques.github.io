---
export function getStaticPaths() {
	return [
		{ params: { slug: 'control-algorithms-and-diagnostics' } },
		{ params: { slug: 'cross-functional-technical-delivery' } },
		{ params: { slug: 'embedded-control-system-architecture' } },
	];
}

return Astro.redirect(`${import.meta.env.BASE_URL}work/`, 301);
---
