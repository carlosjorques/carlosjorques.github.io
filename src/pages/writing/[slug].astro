---
import { getCollection, render } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import ContactCTA from '../../components/ContactCTA.astro';
import { IDENTITY } from '../../data/identity';

export async function getStaticPaths() {
	const entries = await getCollection('writing', ({ data }) => !data.draft);
	return entries.map((entry) => ({
		params: { slug: entry.id.replace(/\.md$/, '') },
		props: { entry },
	}));
}

const { entry } = Astro.props;
const { Content } = await render(entry);
const baseUrl = import.meta.env.BASE_URL;
const normalizedBase = baseUrl.endsWith('/') ? baseUrl : `${baseUrl}/`;
const withBase = (href: string) =>
	href.startsWith('http') || href.startsWith('mailto:') || href.startsWith('#')
		? href
		: `${normalizedBase}${href.replace(/^\//, '')}`;
const label = entry.data.draft
	? 'Planned article placeholder'
	: entry.data.publishDate.toLocaleDateString('en', {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		});

const publishedDate = entry.data.publishDate.toISOString();
const metadata = [entry.data.category, entry.data.readingTime].filter(Boolean);
---

<Layout title={entry.data.title} description={entry.data.description}>
	<article class="writing-post">
		<header class="writing-post__hero" aria-labelledby="article-title">
			<div class="page-container writing-post__container">
				<p class="writing-post__category">{entry.data.category}</p>
				<h1 id="article-title" class="hero-title">{entry.data.title}</h1>
				{entry.data.subtitle && <p class="writing-post__subtitle">{entry.data.subtitle}</p>}
				<p class="writing-post__lead">{entry.data.description}</p>
				<p class="writing-post__meta">
					<time datetime={publishedDate}>{label}</time>
					{metadata.map((item) => <span>{item}</span>)}
				</p>
				<ul class="tag-list writing-post__tags" aria-label="Article tags">
					{entry.data.tags.map((tag) => <li class="tag">{tag}</li>)}
				</ul>
				{
					entry.data.heroImage && (
						<figure class="writing-post__hero-figure">
							<img src={withBase(entry.data.heroImage.src)} alt={entry.data.heroImage.alt} />
						</figure>
					)
				}
			</div>
		</header>

		<section class="writing-post__body" aria-label="Article content">
			<div class="page-container writing-post__container prose">
				<Content />
			</div>
		</section>

		<section class="writing-post__related" aria-labelledby="article-related-title">
			<div class="page-container writing-post__container">
				<h2 id="article-related-title">Related proof</h2>
				<div class="writing-post__related-links" aria-label="Related article links">
					<a
						href={`${import.meta.env.BASE_URL}work/#selected-projects`}
						data-analytics-event="related_proof_click"
						data-cta-id="article-related-work"
						data-cta-placement="writing-article"
						data-cta-intent="proof"
						data-source-section="article-related-proof"
					>
						View Work
					</a>
					<a
						href={`${import.meta.env.BASE_URL}cv/#expertise`}
						data-analytics-event="related_proof_click"
						data-cta-id="article-related-cv"
						data-cta-placement="writing-article"
						data-cta-intent="formal-proof"
						data-source-section="article-related-proof"
					>
						View CV expertise
					</a>
					<a
						href={`${import.meta.env.BASE_URL}contact/`}
						data-analytics-event="cta_click"
						data-cta-id="article-related-contact"
						data-cta-placement="writing-article"
						data-cta-intent="contact"
						data-source-section="article-related-proof"
					>
						Start a technical discussion
					</a>
				</div>
			</div>
		</section>

		<footer class="writing-post__footer">
			<div class="page-container writing-post__container">
				<p class="writing-post__author">{IDENTITY.name} — {IDENTITY.title}</p>
				<nav class="writing-post__footer-links" aria-label="Article footer links"><a href={withBase('writing/')}>More writing</a><a href={withBase('about/')}>About</a><a href={withBase('contact/')}>Working on something similar? Get in touch</a></nav>
				<ContactCTA
					title="Want to discuss the engineering context behind this article?"
					body="For roles, collaborations, or technical conversations around embedded controls, diagnostics, validation, and physical-system software, use the contact page."
					secondaryHref="writing/"
					secondaryLabel="Browse writing"
				/>
			</div>
		</footer>
	</article>

	<nav class="writing-post__nav" aria-label="Writing navigation">
		<div class="page-container writing-post__container">
			<a href={withBase('writing/')}>Back to latest articles</a>
		</div>
	</nav>
</Layout>

<style>
	.writing-post {
		background: var(--color-page);
		overflow-x: clip;
	}

	.writing-post .writing-post__container {
		box-sizing: border-box;
		width: min(calc(100% - 5rem), var(--content-width));
		max-width: 100%;
		margin-inline: auto;
	}

	.writing-post__hero {
		border-bottom: 1px solid var(--color-border-soft);
		padding-block: clamp(3rem, 5vw, 4.6rem) clamp(1.55rem, 3vw, 2.25rem);
	}

	.writing-post__hero .writing-post__container {
		display: grid;
		gap: 1rem;
	}

        .writing-post__category {
                max-width: 100%;
                margin: 0;
                color: var(--color-accent-text);
		font-family: var(--font-mono);
		font-size: var(--type-eyebrow-size);
		font-weight: var(--type-eyebrow-weight);
		letter-spacing: var(--type-eyebrow-tracking);
                line-height: var(--type-eyebrow-line);
                text-transform: uppercase;
                overflow-wrap: anywhere;
			color: var(--color-muted);
			font-size: .82rem;
			font-weight: 600;
			letter-spacing: .08em;
			line-height: 1.4;
        }

	.writing-post .hero-title {
		max-width: min(100%, 20ch);
		margin: 0;
		font-size: clamp(2.6rem, 3.8vw, 3.5rem);
		font-weight: 800;
		line-height: 1.02;
		letter-spacing: -0.05em;
		hyphens: auto;
		overflow-wrap: normal;
		word-break: normal;
	}

	.writing-post__lead {
		max-width: 820px;
		width: 100%;
		color: var(--color-body-text);
		font-size: clamp(1rem, 1.15vw, 1.12rem);
		font-weight: 400;
		letter-spacing: var(--type-body-tracking);
		line-height: 1.55;
		overflow-wrap: anywhere;
	}

	.writing-post__subtitle {
		max-width: 760px;
		width: 100%;
		color: var(--color-text);
		font-family: var(--font-display);
		font-size: clamp(1.55rem, 2.4vw, 2.2rem);
		font-weight: 500;
		letter-spacing: -0.035em;
		line-height: 1.08;
		overflow-wrap: anywhere;
	}

	.writing-post__meta {
		display: flex;
		flex-wrap: wrap;
		gap: 0.35rem 0.7rem;
		min-width: 0;
		max-width: 100%;
		margin: 0;
		color: var(--color-muted);
		font-family: var(--font-mono);
		font-size: var(--type-meta-size);
		font-weight: var(--type-meta-weight);
		letter-spacing: var(--type-meta-tracking);
		line-height: var(--type-meta-line);
		text-transform: uppercase;
		overflow-wrap: anywhere;
	}

	.writing-post__meta time,
	.writing-post__meta span {
		min-width: 0;
		max-width: 100%;
		overflow-wrap: anywhere;
	}

	.writing-post__meta span::before {
		content: '/';
		margin-right: 0.7rem;
		color: var(--color-border);
	}

        .writing-post__tags {
                gap: 0.35rem;
                min-width: 0;
                max-width: 100%;
                padding-left: 0;
        }

	.writing-post__tags .tag {
		max-width: 100%;
		white-space: normal;
		overflow-wrap: anywhere;
	}

	.writing-post__hero-figure {
		margin: clamp(1.3rem, 3vw, 2rem) 0 0;
		overflow: hidden;
		border: 1px solid var(--color-border-soft);
		border-radius: var(--radius);
		background: var(--color-card-bg);
	}

        .writing-post__hero-figure img {
                display: block;
                width: 100%;
                aspect-ratio: 16 / 7;
                object-fit: cover;
	}

	.writing-post__body {
		padding-block: clamp(2.4rem, 5vw, 3.6rem);
	}

	.writing-post__body :global(.prose) {
		max-width: 100%;
		width: 100%;
		overflow-wrap: anywhere;
	}

	.writing-post__body :global(.prose > * + *) {
		margin-top: 1.1rem;
	}

	.writing-post__body :global(.prose h2) {
		margin-top: clamp(1.8rem, 4vw, 2.6rem);
		max-width: none;
		color: var(--color-text);
		font-size: clamp(1.35rem, 1.8vw, 1.72rem);
		letter-spacing: -0.03em;
		line-height: 1.12;
		overflow-wrap: normal;
	}

	.writing-post__body :global(.prose h3) {
		margin-top: clamp(1.35rem, 3vw, 1.85rem);
		max-width: none;
		color: var(--color-text);
		font-size: clamp(1.12rem, 1.35vw, 1.28rem);
		letter-spacing: -0.02em;
		line-height: 1.15;
		overflow-wrap: normal;
	}

	.writing-post__body :global(.prose p),
	.writing-post__body :global(.prose li) {
		color: var(--color-body-text);
		font-size: clamp(0.95rem, 0.92vw, 1.02rem);
		line-height: 1.65;
		overflow-wrap: anywhere;
	}

	.writing-post__body :global(.prose a) {
		color: var(--color-link);
		font-weight: 700;
		text-decoration-thickness: 1px;
		text-underline-offset: 0.2em;
	}

	.writing-post__body :global(.prose a:hover),
	.writing-post__body :global(.prose a:focus-visible) {
		color: var(--color-link-hover);
	}

        .writing-post__body :global(.article-callout),
        .writing-post__body :global(.metric-card),
        .writing-post__body :global(.article-figure),
        .writing-post__body :global(.article-cta) {
                box-sizing: border-box;
                min-width: 0;
                max-width: min(100%, calc(100vw - 48px));
		border: 1px solid var(--color-card-border);
		border-radius: var(--radius);
		background: var(--color-card-bg);
		overflow-wrap: anywhere;
	}

	.writing-post__body :global(.article-callout) {
		display: grid;
		gap: 0.85rem;
		margin-block: clamp(1.4rem, 3vw, 2rem);
		padding: clamp(1rem, 2.6vw, 1.4rem);
	}

	.writing-post__body :global(.article-callout h2),
	.writing-post__body :global(.article-callout h3) {
		margin: 0;
		max-width: none;
		font-size: clamp(1.12rem, 1.45vw, 1.32rem);
		letter-spacing: -0.02em;
		line-height: 1.1;
		overflow-wrap: normal;
	}

	.writing-post__body :global(.article-callout ul) {
		display: grid;
		gap: 0.55rem;
		margin: 0;
		padding-left: 1.2rem;
	}

	.writing-post__body :global(.article-callout li) {
		margin: 0;
	}

	.writing-post__body :global(.process-list) {
		display: grid;
		gap: 0.65rem;
		margin-block: clamp(1.2rem, 3vw, 1.8rem);
		padding: 0;
		list-style: none;
		counter-reset: process-step;
	}

	.writing-post__body :global(.process-list li) {
		counter-increment: process-step;
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 0.75rem;
		align-items: start;
		margin: 0;
		border: 1px solid var(--color-border-soft);
		border-radius: var(--radius);
		background: var(--color-card-bg);
		padding: 0.85rem 1rem;
	}

	.writing-post__body :global(.process-list li::before) {
		content: counter(process-step, decimal-leading-zero);
		display: inline-grid;
		place-items: center;
		min-width: 2.35rem;
		border-radius: calc(var(--radius) * 0.5);
		background: var(--color-text);
		color: var(--color-page);
		font-family: var(--font-mono);
		font-size: var(--type-chip-size);
		font-weight: 750;
		letter-spacing: 0;
		line-height: 1;
		padding: 0.42rem 0.46rem;
	}

	.writing-post__body :global(.metric-grid) {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(min(100%, 210px), 1fr));
		gap: 0.75rem;
		margin-block: clamp(1.2rem, 3vw, 1.8rem);
	}

	.writing-post__body :global(.metric-card) {
		display: grid;
		gap: 0.45rem;
		margin: 0;
		padding: clamp(0.95rem, 2.4vw, 1.2rem);
	}

	.writing-post__body :global(.metric-card strong) {
		color: var(--color-accent-text);
		font-family: var(--font-display);
		font-size: clamp(1.55rem, 3vw, 2.35rem);
		font-weight: 600;
		letter-spacing: -0.055em;
		line-height: 0.98;
	}

	.writing-post__body :global(.metric-card span) {
		color: var(--color-muted);
		font-family: var(--font-mono);
		font-size: var(--type-chip-size);
		font-weight: var(--type-chip-weight);
		letter-spacing: var(--type-chip-tracking);
		line-height: var(--type-chip-line);
		text-transform: uppercase;
	}

	.writing-post__body :global(.comparison-table) {
		margin-block: clamp(1.4rem, 3vw, 2rem);
		overflow-x: auto;
		border: 1px solid var(--color-border-soft);
		border-radius: var(--radius);
		background: var(--color-card-bg);
	}

	.writing-post__body :global(.comparison-table table) {
		width: 100%;
		min-width: 680px;
		border-collapse: collapse;
	}

	.writing-post__body :global(.comparison-table th),
	.writing-post__body :global(.comparison-table td) {
		border-bottom: 1px solid var(--color-border-soft);
		padding: 0.85rem 1rem;
		text-align: left;
		vertical-align: top;
	}

	.writing-post__body :global(.comparison-table th) {
		color: var(--color-text);
		font-family: var(--font-mono);
		font-size: var(--type-chip-size);
		font-weight: 750;
		letter-spacing: 0;
		line-height: var(--type-chip-line);
		text-transform: uppercase;
	}

	.writing-post__body :global(.comparison-table td) {
		color: var(--color-body-text);
		font-size: var(--type-compact-size);
		line-height: var(--type-compact-line);
	}

	.writing-post__body :global(.comparison-table tr:last-child th),
	.writing-post__body :global(.comparison-table tr:last-child td) {
		border-bottom: 0;
	}

	.writing-post__body :global(.article-figure) {
		margin-block: clamp(1.5rem, 4vw, 2.4rem);
		overflow: hidden;
	}

        .writing-post__body :global(.article-figure img) {
                display: block;
                width: 100%;
                background: var(--color-surface-soft);
	}

	.writing-post__body :global(.article-figure figcaption) {
		border-top: 1px solid var(--color-border-soft);
		color: var(--color-muted);
		font-size: var(--type-compact-size);
		line-height: var(--type-compact-line);
		padding: 0.85rem 1rem 1rem;
	}

	.writing-post__body :global(.image-placeholder__frame) {
		display: grid;
		place-items: center;
		min-height: clamp(13rem, 32vw, 20rem);
		padding: clamp(1.1rem, 3vw, 1.6rem);
		background:
			linear-gradient(135deg, color-mix(in srgb, var(--color-surface-soft) 78%, transparent), transparent),
			var(--color-card-bg);
		color: var(--color-muted);
		font-family: var(--font-mono);
		font-size: var(--type-chip-size);
		font-weight: var(--type-chip-weight);
		letter-spacing: 0;
		line-height: var(--type-chip-line);
		text-align: center;
		text-transform: uppercase;
	}

	.writing-post__body :global(.article-cta) {
		display: grid;
		gap: 0.8rem;
		margin-top: clamp(2rem, 4vw, 3rem);
		padding: clamp(1rem, 2.6vw, 1.4rem);
	}

	.writing-post__body :global(.article-cta h2) {
		margin: 0;
		max-width: none;
		font-size: clamp(1.18rem, 1.55vw, 1.42rem);
		letter-spacing: -0.02em;
		line-height: 1.08;
		overflow-wrap: normal;
	}

	.writing-post__body :global(.article-cta p) {
		margin: 0;
	}

	.writing-post__body :global(.article-cta__links) {
		display: flex;
		flex-wrap: wrap;
		gap: 0.55rem 1rem;
		padding: 0;
		list-style: none;
	}

	.writing-post__body :global(.article-cta__links li) {
		margin: 0;
	}

	.writing-post__footer {
		padding-block: 0 clamp(2.6rem, 5vw, 4rem);
	}

	.writing-post__related {
		padding-block: 0 clamp(2rem, 4vw, 3rem);
	}

	.writing-post__related h2 {
		margin: 0 0 0.85rem;
		color: var(--color-accent-text);
		font-family: var(--font-mono);
		font-size: var(--type-meta-size);
		font-weight: var(--type-meta-weight);
		letter-spacing: var(--type-meta-tracking);
		line-height: var(--type-meta-line);
		text-transform: uppercase;
	}

	.writing-post__related-links {
		display: flex;
		gap: 0.75rem 1rem;
		flex-wrap: wrap;
	}

	.writing-post__related-links a {
		color: var(--color-link);
		font-family: var(--font-mono);
		font-size: var(--type-chip-size);
		font-weight: var(--type-chip-weight);
		letter-spacing: var(--type-chip-tracking);
		line-height: var(--type-chip-line);
		text-decoration: none;
		text-transform: uppercase;
		border-bottom: 1px solid transparent;
		transition:
			border-color var(--motion-duration-fast-02) var(--motion-standard-productive),
			color var(--motion-duration-fast-02) var(--motion-standard-productive);
	}

	.writing-post__related-links a:hover,
	.writing-post__related-links a:focus-visible {
		color: var(--color-link-hover);
		border-bottom-color: var(--color-link-hover);
	}

	.writing-post__related-links a:focus-visible {
		outline: 2px solid var(--color-focus);
		outline-offset: 4px;
	}

	.writing-post__nav {
		border-top: 1px solid var(--color-border-soft);
		padding-block: clamp(1rem, 2vw, 1.35rem);
	}

	.writing-post__nav a {
		color: var(--color-text);
		font-family: var(--font-mono);
		font-size: var(--type-chip-size);
		font-weight: 700;
		letter-spacing: var(--type-chip-tracking);
		line-height: var(--type-chip-line);
		text-decoration: underline;
		text-decoration-thickness: 1px;
		text-underline-offset: 0.22em;
		text-transform: uppercase;
	}

	.writing-post__nav a:hover,
	.writing-post__nav a:focus-visible {
		color: var(--color-link-hover);
	}

        @media (max-width: 640px) {
                .writing-post .writing-post__container {
                        width: min(calc(100% - 32px), var(--content-width));
                        max-width: 100%;
                }

		.writing-post .hero-title {
			max-width: 100%;
			font-size: clamp(2.15rem, 10vw, 2.45rem);
			line-height: 1.04;
			letter-spacing: -0.05em;
			overflow-wrap: normal;
			word-break: normal;
		}

		.writing-post__meta span::before {
			margin-right: 0.45rem;
		}
	}
	.writing-post__container {
		min-width: 0;
	}

	.writing-post__hero-figure img,
	.writing-post__body :global(.article-figure img) {
		max-width: 100%;
	}

        @media (max-width: 640px) {
                .writing-post .writing-post__container {
                        width: min(calc(100% - 32px), var(--content-width));
                        max-width: 100%;
                }

		.writing-post__lead,
		.writing-post__subtitle,
		.writing-post__meta,
		.writing-post__tags,
		.writing-post__hero-figure,
		.writing-post__body :global(.prose),
		.writing-post__body :global(.article-callout),
		.writing-post__body :global(.article-figure),
		.writing-post__body :global(.article-cta) {
			box-sizing: border-box;
			inline-size: 100%;
			max-inline-size: 100%;
			width: 100%;
			max-width: 100%;
			min-width: 0;
		}
	}
</style>
